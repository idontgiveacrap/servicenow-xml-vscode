{
  "name": "servicenow-xml",
  "displayName": "ServiceNow XML",
  "description": "Colorize and lint embedded ServiceNow scripts in exported XML; kind-aware XML structure validation; optional Records navigator.",
  "version": "2.9.5",
  "publisher": "local",
  "license": "GPL-3.0-only",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Linters",
    "Programming Languages",
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onLanguage:xml"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "servicenowXml.showKind",
        "title": "ServiceNow XML: Show Document Kind"
      },
      {
        "command": "servicenowXml.revalidate",
        "title": "ServiceNow XML: Revalidate Active Document"
      },
      {
        "command": "servicenowXml.goToRecord",
        "title": "ServiceNow XML: Go to Record"
      },
      {
        "command": "servicenowXml.navigator.enable",
        "title": "ServiceNow XML: Enable Records Navigator"
      },
      {
        "command": "servicenowXml.navigator.refresh",
        "title": "ServiceNow XML: Refresh Records Navigator",
        "icon": "$(refresh)"
      },
      {
        "command": "servicenowXml.navigator.sortBy",
        "title": "ServiceNow XML: Sort Records By…",
        "icon": "$(sort-precedence)"
      },
      {
        "command": "servicenowXml.navigator.filter",
        "title": "ServiceNow XML: Filter Records…",
        "icon": "$(filter)"
      },
      {
        "command": "servicenowXml.navigator.clearFilter",
        "title": "ServiceNow XML: Clear Records Filter",
        "icon": "$(clear-all)"
      },
      {
        "command": "servicenowXml.revealInExplorer",
        "title": "Reveal in Explorer"
      },
      {
        "command": "servicenowXml.revealInFileExplorer",
        "title": "Reveal in File Explorer"
      },
      {
        "command": "servicenowXml.cursor.installHelpers",
        "title": "ServiceNow XML: Install Cursor Helpers"
      },
      {
        "command": "servicenowXml.cursor.runIndexer",
        "title": "ServiceNow XML: Run Repo Indexer"
      },
      {
        "command": "servicenowXml.editJsonStringCode",
        "title": "ServiceNow XML: Edit Embedded Script…"
      },
      {
        "command": "servicenowXml.loadJsonStringDraft",
        "title": "ServiceNow XML: Load Draft for Embedded Script…"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "servicenow-xml",
          "title": "ServiceNow",
          "icon": "media/servicenow.svg"
        }
      ]
    },
    "views": {
      "servicenow-xml": [
        {
          "id": "servicenowXml.records",
          "name": "Records",
          "contextualTitle": "ServiceNow Records",
          "when": "servicenowXml.isSnWorkspace || servicenowXml.hasSnDocument || config.servicenowXml.enabledForAllWindows"
        }
      ]
    },
    "viewsWelcome": {
      "servicenowXml.records": [
        {
          "contents": "Enable the navigator to browse ServiceNow records by name.\n[Enable Records Navigator](command:servicenowXml.navigator.enable)",
          "when": "!config.servicenowXml.navigator.enable"
        },
        {
          "contents": "Records are indexed from XML files in the workspace. This window has no folder open, so there is nothing to index — open the folder containing your exports to browse records.\n\nDiagnostics and the document kind still work on the open file.",
          "when": "config.servicenowXml.navigator.enable && workspaceFolderCount == 0"
        }
      ]
    },
    "menus": {
      "view/title": [
        {
          "command": "servicenowXml.navigator.filter",
          "when": "view == servicenowXml.records",
          "group": "navigation@1"
        },
        {
          "command": "servicenowXml.navigator.clearFilter",
          "when": "view == servicenowXml.records && servicenowXml.recordsFiltered",
          "group": "navigation@2"
        },
        {
          "command": "servicenowXml.navigator.sortBy",
          "when": "view == servicenowXml.records",
          "group": "navigation@3"
        },
        {
          "command": "servicenowXml.navigator.refresh",
          "when": "view == servicenowXml.records",
          "group": "navigation@4"
        }
      ],
      "view/item/context": [
        {
          "command": "servicenowXml.revealInExplorer",
          "when": "view == servicenowXml.records && viewItem == servicenowXml.record",
          "group": "navigation@1"
        },
        {
          "command": "servicenowXml.revealInFileExplorer",
          "when": "view == servicenowXml.records && viewItem == servicenowXml.record",
          "group": "navigation@2"
        }
      ],
      "editor/context": [
        {
          "command": "servicenowXml.editJsonStringCode",
          "when": "editorLangId == xml && resourceScheme == file",
          "group": "1_modification@50"
        }
      ]
    },
    "configuration": {
      "title": "ServiceNow XML",
      "properties": {
        "servicenowXml.enable": {
          "type": "boolean",
          "default": true,
          "description": "Enable ServiceNow XML validation and JS linting once a gate passes (app workspace marker, an export-shaped document, or enabledForAllWindows)."
        },
        "servicenowXml.enabledForAllWindows": {
          "type": "boolean",
          "default": false,
          "description": "Bypass the ServiceNow app workspace gate so diagnostics run for every XML file in the window, not just export-shaped ones, and the Records view stays visible. Single export files already lint without this."
        },
        "servicenowXml.formatJavaScript": {
          "type": "boolean",
          "default": true,
          "description": "After XML format, format JavaScript in script-typed XML fields (including scripts nested in update-set payloads). Uses the editor's JavaScript formatter."
        },
        "servicenowXml.formatXmlFirst": {
          "type": "boolean",
          "default": true,
          "description": "When this extension is the XML default formatter, run the next XML formatter before formatting embedded scripts."
        },
        "servicenowXml.lintJavaScript": {
          "type": "boolean",
          "default": true,
          "description": "Lint embedded JavaScript in script CDATA regions."
        },
        "servicenowXml.lintJson": {
          "type": "boolean",
          "default": true,
          "description": "Lint JSON embedded in known ServiceNow XML fields."
        },
        "servicenowXml.ignoreGlobs": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "**/author_elective_update/**"
          ],
          "description": "Glob patterns for XML paths to skip (validation, lint, declaration index, and navigator)."
        },
        "servicenowXml.debounceMs": {
          "type": "number",
          "default": 400,
          "description": "Debounce delay (ms) before re-validating on edit."
        },
        "servicenowXml.navigator.enable": {
          "type": "boolean",
          "default": false,
          "description": "Enable the ServiceNow Records navigator. No indexing runs until the view is opened or Go to Record is used."
        },
        "servicenowXml.navigator.excludeDelete": {
          "type": "boolean",
          "default": false,
          "description": "Hide action=DELETE records from the navigator."
        },
        "servicenowXml.navigator.sortBy": {
          "type": "string",
          "default": "mostOpened",
          "enum": [
            "mostOpened",
            "recentlyOpened",
            "recentlyUpdated",
            "sysModCount",
            "name"
          ],
          "enumDescriptions": [
            "Most opened (open count; table order by sum of opens)",
            "Recently opened (last open time; table order by most recent child)",
            "Recently updated (file mtime; table order by newest child)",
            "sys_mod_count (ServiceNow update count; table order by max child)",
            "Alphabetical by table then display name"
          ],
          "description": "Sort order for Records navigator tables and records."
        },
        "servicenowXml.cursorHelpers.enable": {
          "type": "boolean",
          "default": true,
          "description": "In Cursor only: install ServiceNow MCP servers, user rules, and the repo indexer on activation. No-op in VS Code."
        },
        "servicenowXml.cursorHelpers.installIndexHook": {
          "type": "boolean",
          "default": true,
          "description": "In Cursor only: idempotently add a user sessionStart hook that refreshes index.json when stale for ServiceNow export workspaces."
        },
        "servicenowXml.cursorHelpers.pythonPath": {
          "type": "string",
          "default": "python",
          "description": "Python executable used for the repo indexer, local MCP servers (DB schema, scripting reference), and sessionStart hook."
        }
      }
    },
    "configurationDefaults": {
      "[xml]": {
        "editor.defaultFormatter": "local.servicenow-xml"
      }
    },
    "grammars": [
      {
        "scopeName": "servicenow-xml.injection",
        "path": "./syntaxes/servicenow-xml.injection.tmLanguage.json",
        "injectTo": [
          "text.xml"
        ],
        "embeddedLanguages": {
          "meta.embedded.block.javascript": "javascript",
          "source.js": "javascript"
        }
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run build",
    "build": "node esbuild.js --production",
    "watch": "node esbuild.js --watch",
    "package": "npm run build && vsce package --no-dependencies --allow-missing-repository",
    "compile": "npm run build",
    "test": "node scripts/smoke-record-name.js && node scripts/smoke-catalog-cache.js && node scripts/smoke-script-declarations.js && node scripts/smoke-classify.js && node scripts/smoke-json-string-editor.js && node scripts/smoke-embedded-layers.js && node scripts/smoke-scripts.js && node scripts/smoke-lint-bundle.js && tsc --noEmit"
  },
  "devDependencies": {
    "@types/eslint": "^9.6.1",
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^3.2.0",
    "esbuild": "^0.28.1",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "eslint": "^8.57.1",
    "eslint-plugin-servicenow": "^2.0.0",
    "fast-xml-parser": "^5.10.1",
    "jsonc-parser": "^3.3.1",
    "minimatch": "^9.0.5"
  }
}
